# Field Tracker 版本更新日志

> 版本格式: `v主版本.次版本.修订号+内部版本号`
> 更新日期: 2026-07-24

---

## v1.1.0 — 管理后台大修（2026-07-24）

### 🐛 Bug 修复（管理后台）
- **电子围栏切模式地图空白** — `setFenceMode` 不再摧毁重建地图
- **画圆不显示圆周** — `AMap.Circle` → 72边形 Polygon 绕过高德渲染 bug
- **保存围栏后地图消失** — `setTimeout` 碰运气 → `async/await` 精确时序
- **查看/编辑已有围栏不跳转视野** — 加 `setCenter` + `setZoom`
- **📍按钮点不动** — 内联 `onclick` → 事件委托
- **编辑围栏整页重载** — 检测是否已在围栏页，避免重复加载
- **删除围栏地图崩溃** — `async/await` 替代 `setTimeout`

### ⚡ 优化（管理后台）
- **浏览器刷新回到当前标签页** — hash routing，不再跳回仪表盘
- **登录页不再闪现** — CSS 默认隐藏 + JS 按需显示
- **顶栏显示版本号** — 从 `admin-version.json` 读取
- **版本管理体系** — `admin-version.json` + 备份目录 + rollback 脚本

---

## v1.0.48+48 — 安全审计修复（2026-07-25）

### 🔴 安全修复
- **API Key硬编码移除** — AMap密钥改为 `--dart-define` 编译注入
- **JWT/DB默认密码移除** — 启动时校验环境变量，缺失拒绝启动
- **隧道地址硬编码移除** — 移除 serveo 兜底，全部通过远程配置
- **Redis降级保护** — 操作加 try-catch，故障不中断连接
- **管理后台预填密码移除** — admin.html登录页移除 `value="123456"`
- **文件上传魔数校验** — 文件头4字节校验，防伪造MIME上传非图片

### 🟠 严重修复
- **围栏事件事务保护** — auto-check 用 BEGIN/COMMIT/ROLLBACK
- **离线打卡数据不丢失** — 失败保留剩余，只清除确已成功的
- **COALESCE无法置空修复** — 4个PUT路由统一 `'key' in req.body` 模式
- **manager角色权限** — 经理可查看所管理部门下属员工客户
- **里程统计不再返回随机数** — 改为0.0+功能开发中标记
- **考勤时间改为分钟数比较** — 支持跨天规则
- **注册审计日志持久化** — 新建 audit_logs 表
- **report.ts JSON.parse保护** — 单条异常不导致全量500
- **删除用户改用UUID** — 而非手机号
- **部门PUT返回补managerId**

### 🟡 一般修复
- **定位DB写入加日志** — 空catch改 console.warn
- **APK下载路由动态查找** — 按mtime找最新 .apk
- **localhost:3000硬编码** — 改为 process.env.PORT
- **考勤规则PUT补wifi_bssid**

---

## v1.0.46+43 — 构建完成（2026-07-23）

### 🎯 Bug 修复
- **隧道地址导致登录不了** — 构建前自动更新 env_config.dart 默认地址为最新隧道
- **隧道502网关错误** — 重建新隧道，app-version.json 同步更新

### ⚡ 优化
- **首次安装权限引导** — 自动弹出三步引导页（定位/电池优化/后台），点按钮一键跳转系统设置
- **版本号显示** — 首页底部清晰显示版本号，反馈bug时一目了然

---

## v1.0.45+42 — 构建完成（2026-07-23）

### 🎯 Bug 修复
- **轨迹双线** — 关闭原生HTTP保底上传通道（双通道写同一张表导致轨迹回放两条线重叠）
- **轨迹数据丢失** — `_flushBuffer` 批量平均改为逐个独立上传，隧道断线恢复后轨迹细节不丢
- **轨迹回放地图底图消失** — `_error` 状态不再跳过 `AMapWidget` 加载，地图底图始终显示
- **增量模式距离重复计算** — 跳过重叠段距离累加，总距离不再偏高

### ⚡ 优化
- **前台才刷新** — 轨迹回放页面监听 `AppLifecycleState`，锁屏/切后台停止定时刷新（省电省流量）
- **返回前台立即刷新** — 解锁屏后立即触发一次 `_loadTrack`，不等15秒定时器
- **自动刷新间隔** — 3秒 → 15秒，降低服务端请求压力
- **`_isLoadingTrack` 去重锁加固** — 移到 `finally` 块，避免极端情况锁死
- **死字段清理** — 删除 `_lastKnownLocation`、`_gpsLostSince`（只赋值未读取）

### 🗑 废弃
- **原生HTTP保底上传** — `nativeBuffer`/`flushNativeBuffer` 代码保留但关闭（`nativeUploadEnabled=false`），方便未来恢复

---

## v1.0.44+41

### 🎯 Bug 修复
- 隧道地址变更时推送通知到APP
- 围栏检测自动触发（API端点）

### ⚡ 优化
- 单一AMap架构：ForegroundService统一管理GPS采集，Flutter AMap插件禁用定位功能
- GPS动态间隔：静止3分钟→60秒采集，运动→3秒
- 隧道预切换系统：隧道运行12h后自动建备用隧道，断线秒切

---

## v1.0.40+39

### 🎯 Bug 修复
- GPS看门狗：连续无数据时自动重启定位
- 轨迹断点续传

---

## v1.0.15

### 🎯 Bug 修复
- 用户账号体系重构：移除 `-1` 降级逻辑
- 使用 `user_code` 工号作为人类友好标识
- 登录只走数据库，不硬编码测试用户

---

## v1.0.10

### 🎯 Bug 修复
- 通知渠道未初始化导致闪退（华为P50E）

---

## v1.0.7

### 🎯 Bug 修复
- 轨迹回放小蓝点动画
- 清除测试数据
- 地图缩放优化

---

## v1.0.5

### 🎯 Bug 修复
- 版本号修正（pubspec.yaml 未同步更新）
- 更新下载解析包错误
- 直链下载+进度显示

---

## v1.0.4

### ⚡ 优化
- 定位精度大幅提升
- 精度显示
- 高德Web搜索集成

---

## v1.0.3

### 🎯 Bug 修复
- 重复提示弹窗修复
- 版本号显示

---

## v1.0.2

### ⚡ 优化
- 下载进度条+百分比
- 优先使用图床下载
- 定位回归 Geolocator

---

## 历史版本（初步搭建）

- v1.0.1 — 基础框架搭建
- v1.0.0 — 项目初始化
